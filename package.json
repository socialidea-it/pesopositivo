{
  "name": "my-project",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "build": "rm -rf ./dist && parcel build"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.6.2",
    "parcel": "latest",
    "sass": "1.55.0",
    "sass-loader": "13.2.0"
  },
  "dependencies": {
    "bootstrap": "^5.2.2"
  }
}
